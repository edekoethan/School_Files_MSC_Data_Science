#Basic Arithmetic
#A
10 ^ 2 + ((3 * 60 ) / 8 ) - 3
#B
(5 ^ 3) * (6 - 2) / (61 - (3 + 4))
#C
(2 ^ (2+1)) - (4 + (64 ^ (-2 ^ (2.25) ^ - 0.25)))
#D
((0.44 * (1 - 0.44)) / 34) ^ (1 / 2)
sqrt(x=3)

#   A sequence of number from 2 to 6, stored in a vector named a. 
vector_a = c(2,3,4,5,6)
#  Atwofold repetition of the vector c(2, -5.1, -33), stored in a vector named b.
vector_b = rep(c(2,-5.1,-33), times=2)
vector_b


# Extract the first and last elements of the vectors created in step (1), and store them in new objects 
#named a1 and b1.

vector_a_last = vector_b[length(vector_b)]
vector_a_before_last = vector_b[length(vector_b)-1]

vector_a1 = c(vector_a_last, vector_a_before_last)
vector_a1

vector_b = rep(c(2,-5.1,-33), times=2)
vector_b



#Create new objects by omitting the first and last elements of the vectors from step (1). Store these 
#as a2 and b2










